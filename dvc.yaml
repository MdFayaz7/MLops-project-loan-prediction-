stages:
  train:
    cmd: python -m src.pipeline.training_pipeline
    deps:
      - data/raw/loan_data.csv
      - config/config.yaml
      - src/components/
      - src/pipeline/training_pipeline.py
    outs:
      - data/processed/
      - artifacts/model.pkl
